name: Benchmarks
on:
  - push

jobs:
  c:
    name: C Benchmark
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Build C
      run: gcc scripts/fibonacci.c -o /tmp/fibonacci.x

    - name: Run script
      run: /tmp/fibonacci.x 55
